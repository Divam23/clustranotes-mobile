import 'package:flutter/widgets.dart';

class CollectionDefaultThumbnail extends StatelessWidget{
  const CollectionDefaultThumbnail({super.key});
  
  @override
  Widget build(BuildContext context){
    return Container();
  }
}
